#include<stdio.h>
#include<math.h>

int main()
{
    double radius;
    int testCase;

    scanf("%d", &testCase);

    for(int i=0; i<testCase; i++)
    {
        scanf("%lf", &radius);

        printf("Case %d: %.2lf", i, (radius*radius(4 - math.pi)));
    }

    return 0;
}
